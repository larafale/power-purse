var Promise  = require('bluebird');

module.exports = function(collections, models) {

  class AccountsLib {

    register() {
      return new Promise(function() {
      });
    }

    find() {
      return new Promise(function() {
      });
    }
  }
  
  return AccountsLib
}

